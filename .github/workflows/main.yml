name: Crypto Currency Environment Build

on:
  create:
    tags:
      - v*

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      AWS_ACCESS_KEY_ID: ${{secrets.AWS_ACCESS_KEY_ID}}
      AWS_SECRET_ACCESS_KEY: ${{secrets.AWS_SECRET_ACCESS_KEY}}
    steps:
      - uses: actions/checkout@v4
